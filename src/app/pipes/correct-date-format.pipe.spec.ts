import { CorrectDateFormatPipe } from './correct-date-format.pipe';

describe('CorrectDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CorrectDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
